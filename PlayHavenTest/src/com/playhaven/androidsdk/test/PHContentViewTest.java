package com.playhaven.androidsdk.test;

import android.test.AndroidTestCase;

import com.playhaven.src.publishersdk.content.PHContent;
import com.playhaven.src.publishersdk.content.PHContentView;

public class PHContentViewTest extends AndroidTestCase {
	public void testContentView() throws Exception {
		//TODO in iOS version we check to make sure it responds to methods.
		//here, we'll just make sure it works to create it
		//UPDATE we actually can't test without an activity..
		//PHContentView view = new PHContentView(new PHContent());
	}
}
